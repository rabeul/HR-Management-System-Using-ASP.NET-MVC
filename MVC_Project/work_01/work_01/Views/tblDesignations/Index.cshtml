@model IEnumerable<work_01.Models.tblDesignation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblDepartment.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Designation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblDepartment.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesigCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesigCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesigCode })
        </td>
    </tr>
}

</table>
